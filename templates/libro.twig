<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ libro.titulo }}</title>
    <link href="/styles.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1>{{ libro.titulo }}</h1>
        <img src="{{ libro.imagen_url }}" alt="Portada de {{ libro.titulo }}" class="portada">
        <p><strong>Autor:</strong> {{ libro.nombre_autor }}</p>
        <p><strong>Descripción:</strong> {{ libro.descripcion }}</p>
        <p><strong>Formato:</strong> {{ libro.formato }}</p>
        <p><strong>Número de páginas:</strong> {{ libro.num_paginas }}</p>
        <p><strong>Fecha de publicación:</strong> {{ libro.fecha_publicacion }}</p>
        <p><strong>Número de calificaciones:</strong> {{ libro.num_calificaciones }}</p>
        <p><strong>Número de reseñas:</strong> {{ libro.num_resenas }}</p>
        <p><strong>Categoría:</strong> {{ libro.nombre_categoria }}</p>
        <a href="{{ libro.libro_url }}" target="_blank">Ver libro</a>
        <br><br>
        <a href="/categorias">Volver a Categorías</a>
    </div>
</body>
</html>
